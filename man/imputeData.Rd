% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputeData.R
\name{imputeData}
\alias{imputeData}
\title{Advanced Dichotomous Data Imputation with Multiple Methods}
\usage{
imputeData(data, method = "mi", ...)
}
\arguments{
\item{data}{Data frame or matrix containing missing values to be imputed.}

\item{method}{Imputation method to use. One of:
\code{"mode"}, \code{"knn"}, \code{"random_forest"}, \code{"decision_tree"}, \code{"svm"},
\code{"ann"}, \code{"mi"}, \code{"logistic"} (default: \code{"mi"})}

\item{...}{Additional arguments passed to underlying imputation functions}
}
\value{
A complete dataset with missing values imputed, matching the input structure.
Returns the original data unchanged if no missing values are detected.
}
\description{
Provides a unified interface for various statistical and machine learning imputation methods.
Supports both univariate and multivariate imputation with automatic method validation
and comprehensive error handling. Part of the DataPreprocessingR package ecosystem.
}
\note{
The function automatically validates method suitability based on data completeness.
Methods requiring complete features (\code{knn}, \code{decision_tree}, \code{svm}, \code{ann}, \code{logistic})
will fail if no complete columns exist. Use \code{check_missing()} to verify method availability.
}
\section{Method Details}{

\itemize{
\item \strong{\code{mode}}: Simple majority voting for categorical data
\item \strong{\code{knn}}: k-Nearest Neighbors using VIM package
\item \strong{\code{random_forest}}: MissForest algorithm for mixed data types
\item \strong{\code{decision_tree}}: CART-based imputation using rpart
\item \strong{\code{svm}}: Support Vector Machines for regression/classification
\item \strong{\code{ann}}: Artificial Neural Networks with nnet
\item \strong{\code{mi}}: Multiple Imputation by Chained Equations (MICE)
\item \strong{\code{logistic}}: Logistic regression for binary outcomes
}
}

\section{Requirements}{

Feature-based methods (knn, decision_tree, svm, ann, logistic) require
at least one complete column. Use \code{check_missing()} to diagnose data patterns
before imputation.
}

\examples{
\dontrun{
# Create dataset with missing values
df <- data.frame(
  A = c(1, 2, 3, 4, 5),
  B = c(NA, 2, NA, 4, 5),
  C = c(1, 2, 3, NA, NA)
)

# Check missing patterns first
missing_info <- check_missing(df)

# Perform imputation using random forest
imputed_data <- impute_data(df, method = "random_forest")

# Perform imputation with custom parameters
imputed_data <- impute_data(df, method = "knn", k = 3)
imputed_data <- impute_data(df, method = "mi", m = 10, maxit = 20)
}

}
\seealso{
\itemize{
\item \code{\link[=check_missing]{check_missing()}} for data diagnostics before imputation
\item \code{\link[mice:mice]{mice::mice()}} for multiple imputation implementation
\item \code{\link[missForest:missForest]{missForest::missForest()}} for random forest imputation
\item \code{\link[VIM:kNN]{VIM::kNN()}} for k-nearest neighbors imputation
\item \code{\link[rpart:rpart]{rpart::rpart()}} for decision tree models
\item \code{\link[e1071:svm]{e1071::svm()}} for support vector machines
\item \code{\link[nnet:nnet]{nnet::nnet()}} for neural networks implementation
}
}
\author{
Fei Wang, State Key Laboratory of Cognitive Science and Mental Health,
Institute of Psychology, Chinese Academy of Sciences (2025-08-31)
}
